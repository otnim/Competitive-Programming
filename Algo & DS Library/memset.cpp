#include <bits/stdc++.h>
using namespace std;
#define nl   '\n'



int main(){
    //freopen("ip.txt", "r", stdin);
    int T; T = 1;
    for(int tc = 1; tc <= T; tc++){
        long long ara[5];
        memset(ara, 127, sizeof ara); /// max value assign hoy
        cout<<ara[0]<<nl;

        memset(ara, 128, sizeof ara); /// min value assign hoy
        cout<<ara[0]<<nl;
    }
}


